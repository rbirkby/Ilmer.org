{% include "head" %}

{% include "site-header" %}
<main>
  {% if image %}
    <figure>
      <img src="{{ image | sitePath }}" alt="{{ title }}">
    </figure>
  {% endif %}
  <section class="article" data-lightbox-all="{{ lightbox | default: false }}">

    {% if breadcrumb %}
      <p class="breadcrumb">
        <a href="{{ breadcrumbUrl }}">← {{ breadcrumb }}</a>
      </p>
    {% endif %}

    <h1>{{ title }}</h1>

    <p>{{ description }}</p>

    {{ content }}
  </section>

  <div id="image-lightbox" popover>
    <button
      popovertarget="image-lightbox"
      class="lightbox-close"
      aria-label="Close lightbox">&times;</button>
    <img id="lightbox-image" alt="" />
    <p id="lightbox-caption"></p>
  </div>
</main>
{% include "footer" %}
