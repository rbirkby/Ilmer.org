{% include "head" %}

<link
  rel="stylesheet"
  href="{{ '/assets/css/timeline.css' | cacheBust }}"
  media="all">

{% include "site-header" %}
<main>
  <section>
    {% include "breadcrumb.liquid" %}
    <h1>{{ title }}</h1>

    <p>{{ description }}</p>

    {{ content }}
  </section>
</main>
{% include "footer" %}
