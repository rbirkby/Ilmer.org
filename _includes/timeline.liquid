{% include "head" %}

{% include "site-header" %}
<main>
  <section>
    {% include "breadcrumb.liquid" %}
    <h1>{{ title }}</h1>

    <p>{{ description }}</p>

    {{ content }}
  </section>
</main>
{% include "footer" %}
