{% include "head" %}

{% include "site-header" %}
<main class="archive">
  <section class="article">
    {% include "breadcrumb.liquid" %}

    <h1 class="minute-heading">{{ title }}</h1>
    <p>{% if description %}{{ description }}{% else %}{{ tagline }}{% endif %}</p>

    {{ content }}
  </section>
</main>
{% include "footer" %}
